The patch-0.45.5-pre*.gz files are all against 0.45.4.  They are not
cumulative, so you have to back out the last one before applying the
new one.

This time around I'm going to try making cumulative patches as well.
pre1-pre2.gz is just that.
